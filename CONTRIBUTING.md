# コントリビューションガイド

## ブランチ名

`codex`は含めず、変更内容が分かる名前にします。

例：

- `feature/minecraft-join-guide`
- `docs/update-community-rules`
- `fix/broken-navigation`

## コミット・PR

コミットメッセージとPRタイトル・本文は日本語で記載します。

## 記事作成時の確認

- 初心者が読んでも手順が分かるか
- 古いバージョンを断定していないか
- 内部情報や個人情報が含まれていないか
- 見出しだけで内容を探せるか
- 問題発生時の次の行動が書かれているか

## 公開リポジトリへ置かない情報

- APIキー、アクセストークン、パスワード、秘密鍵
- 内部IPアドレス、内部向けホスト名、管理画面URL
- 個人情報、監査ログ、障害発生時の生ログ
- バックアップ保存先や管理者専用の接続情報

手順例には実値を使わず、`example.com`や文書用に予約されたIPアドレスを使用してください。内部構成、復旧手順、運営判断を含むメモはNotionの運用Runbookで管理します。

## PR作成前の品質チェック

```bash
npm install
npm run check:repository
npm run check
npm run build
```

`check:repository`では、公開できない可能性がある情報と、日本語・英語ドキュメントの内部リンク切れを検査します。検査に失敗した場合は、表示されたファイル名・行番号・理由を確認して修正してください。
