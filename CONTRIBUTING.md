# コントリビューションガイド

## ブランチ名

`codex`は含めず、変更内容が分かる名前にします。

例：

- `feature/minecraft-join-guide`
- `docs/update-community-rules`
- `fix/broken-navigation`

## コミット・PR

コミットメッセージとPRタイトル・本文は日本語で記載します。

## 記事作成時の確認

- 初心者が読んでも手順が分かるか
- 古いバージョンを断定していないか
- 内部情報や個人情報が含まれていないか
- 見出しだけで内容を探せるか
- 問題発生時の次の行動が書かれているか

## アクセシビリティ確認

- リンク、ボタン、フォームをキーボードだけで操作できるか
- `Tab`移動時にフォーカス位置が明確に見えるか
- アイコンだけのボタンに`aria-label`などの名前があるか
- 画像に用途に合った`alt`があるか。装飾画像は空の`alt=""`にする
- ダイアログに`aria-label`または`aria-labelledby`があるか
- 文字と背景のコントラストがダーク・ライト両方で読めるか
- 操作ボタンのタッチ領域が原則44px以上あるか
- Windowsの強制カラーモードやコントラスト強調でも操作対象が見えるか

## 公開リポジトリへ置かない情報

- APIキー、アクセストークン、パスワード、秘密鍵
- 内部IPアドレス、内部向けホスト名、管理画面URL
- 個人情報、監査ログ、障害発生時の生ログ
- バックアップ保存先や管理者専用の接続情報

手順例には実値を使わず、`example.com`や文書用に予約されたIPアドレスを使用してください。内部構成、復旧手順、運営判断を含むメモはNotionの運用Runbookで管理します。

## PR作成前の品質チェック

```bash
npm install
npm run check:repository
npm run check
npm run build
```

`check:repository`では、公開できない可能性がある情報と、日本語・英語ドキュメントの内部リンク切れを検査します。検査に失敗した場合は、表示されたファイル名・行番号・理由を確認して修正してください。

`npm run build`ではAstroの型・コンテンツ検査と本番ビルドに続いて、生成サイトの構造とアクセシビリティを検査します。アクセシビリティ検査だけを再実行する場合は、`dist`生成後に次を実行してください。

```bash
npm run check:accessibility
```

検査対象は、`html[lang]`、ページタイトル、`main`要素、重複ID、画像の`alt`、ボタン名、ダイアログのラベル、別タブで開く外部リンクの`rel`です。失敗時は対象HTML、行番号、理由が表示されます。
