{
  "name": "ivrm-help",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro check && astro build && npm run verify:site",
    "preview": "astro preview",
    "check": "astro check",
    "check:public": "node scripts/check-public-content.mjs",
    "check:links": "node scripts/check-internal-links.mjs",
    "check:repository": "npm run check:public && npm run check:links",
    "verify:site": "node scripts/verify-build.mjs"
  },
  "dependencies": {
    "@astrojs/starlight": "latest",
    "astro": "latest",
    "sharp": "latest"
  },
  "devDependencies": {
    "@astrojs/check": "latest",
    "typescript": "latest"
  },
  "engines": {
    "node": ">=22"
  }
}
